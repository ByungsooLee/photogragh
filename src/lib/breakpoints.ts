/**
 * Shared breakpoint definitions that match the layout.
 * Use these values for both JS matchMedia and styled-components media queries.
 */
export const MOBILE_BREAKPOINT = 760;
export const TABLET_BREAKPOINT = 1100;

/** Desktop min width: TABLET_BREAKPOINT + 1 (desktop starts at 1101px). */
export const DESKTOP_MIN_WIDTH = TABLET_BREAKPOINT + 1;
